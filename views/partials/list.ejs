<div class="container">
  <!-- Columns are always 50% wide, on mobile and desktop -->

  <div class="row" style="text-align:center; margin:2px">

    <% item_table.forEach(function(item) { %>
      <a id="shadow" href="../item/<%= item.i_id %>">
        <div style="margin:5px; padding:0px 5px">
        <div class="col-xs-6">
          <div class="row" style="background-color: #FFFFFF; margin-right:1px; margin-top:3px; padding-top:5px">
            <img src="<%= item.img_link %>" height="50%" width="50%" class="img-circle" style="margin-bottom:5px">
            <br><%= item.i_name %>
          </div>
          <div class="row" style="background-color: #FFFFFF; margin-right:1px; padding-bottom:5px">
            <span>฿<%= item.i_price %>.00</span>
          </div>
        </div>
      </div>
      </a>
    <% }); %>
  </div>
</div>
